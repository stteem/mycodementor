export interface Subscription {
    _id: string;
    plan: string;
    price: number; 
    session_per_month: number;
    createdAt: string;
    updatedAt: string;
}

/*export interface Subscription {
    plan: string;
    value: number; 
    session: number;
}*/